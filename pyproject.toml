[project]
name = "taskapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "apogano", email = "apogano@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.53.0",
]

[project.scripts]
taskapi = "taskapi:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
